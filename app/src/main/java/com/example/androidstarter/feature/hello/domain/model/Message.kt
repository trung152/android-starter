package com.example.androidstarter.feature.hello.domain.model

/**
 * Domain Model - trung lập, không phụ thuộc bất kỳ framework nào.
 * Đây là "sự thật" (truth) của tầng Domain.
 */
data class Message(val content: String)
